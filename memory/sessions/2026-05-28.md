# Sessão — 2026-05-28

## O que foi feito

- **Maqlam Catálogo — Envio de produtos via WhatsApp (feature completa):** carrinho de envio, modal SendProductModal (busca contato, picker instância, progress bar), preview editável por produto
- **Fix endpoints uazapi:** `/message/sendMedia` (inexistente, dava 405) → `/send/media` + `/send/text` com payload correto (`number`/`type`/`file`/`text`) conforme docs.uazapi.com
- **Strip de HTML na descrição** vinda da Olist — eliminou `<p style=...>` aparecendo na legenda do WhatsApp
- **Reescrita de descrição com IA:** edge function `rewrite-product-caption` (GPT-4o-mini) deployada no Supabase (ACTIVE) — botão manual "Reescrever com IA" no preview; corrigido bug do `requireJwtAuth` que não checava `.error`
- **Campo `descricao_whatsapp` em `ai_products`:** migration aplicada — descrição custom persistente, nunca sobrescrita pela sync do Tiny; botão "Salvar como padrão" no modal; `formatCaption` prioriza esse campo
- **Fix UI:** CartPanel não sobrepõe mais a paginação (`pb-24` quando carrinho ativo); removido header duplicado "// APP Maqlam" na rota `/catalogo` (App.tsx)
- **/validar-sessao:** 1 task criada no ClickUp (`86e1kebn4`, subtask de `86e1k40vw`, concluído)

## Decisões

- Reescrita com IA é **manual** (botão), não automática ao abrir o preview — vendedor decide se reescreve
- Descrição custom do WhatsApp em **campo separado** (`descricao_whatsapp`) em vez de editar `descricao_complementar` — evita conflito com a sync do Tiny/Olist que sobrescreve a cada execução
- IA do projeto usa **OpenAI/GPT-4o-mini** (stack unificada via `_shared/openai.ts`), não Gemini/Anthropic

## Em aberto

- Merge dos PRs abertos (#20, #22) — fix header já foi direto no main (commit 84574c3)
- Time entry de 90min não registrado no ClickUp — plano atingiu limite de 40 usos de Advanced Time Tracking

---

## Tasks ClickUp tocadas

- 🆕 `86e1kebn4` — Maqlam — Envio de produtos do catálogo via WhatsApp (concluído)

---

## Sessão 14:00 — Maqlam Catálogo no RAG (hybrid_search) + fix search-knowledge

### O que foi feito
- **Catálogo de produtos no RAG:** migration `cat05` — `hybrid_search()` agora inclui `ai_products` (CTE `prod_rows`, FTS via expression index GIN + semântico). Antes os embeddings existiam mas nunca eram consultados. Filtro só `org_id` (catálogo é dado da org), inativos excluídos.
- **Organização via GSD:** `/gsd-import` do `docs/PLAN-catalogo-rag-hybrid-search.md` → estrutura `.planning/phases/01-catalogo-rag/` → `/gsd-execute-phase 01` (executor + plan-checker + verifier). Fase fechada.
- **embed-knowledge v23** re-deployado (case `ai_products`: nome+SKU+categoria+descrição+preço+promo+estoque). 1657/1657 produtos ativos com embedding (100%).
- **Smoke test:** `hybrid_search('shampoo')` → 18 produtos com nome/preço/descrição corretos.
- **Fix regression (erro 500 em produção):** a migration renomeou parâmetro do RPC (`p_min_similarity` → `p_max_distance`), quebrando o `search-knowledge` que o N8N chamava → HTTP 500. Re-deploy v24 + label 'Produto' no contextBlock. Teste e2e com `agentId 767e8044...` → 200, mode hybrid, "Maq Assistente", contexto OK.
- **/validar-sessao:** comentários em `86e1k40vw` (RAG finalizado) e `86e1k160z` (backend desbloqueado).

### Decisões
- Produtos no RAG sem `visibility`/`agent_ids` — só `org_id` (catálogo é dado da org, todos os agentes enxergam). Ver `decisoes/2026-05.md`.
- Lição: renomear parâmetro de RPC = breaking change para edge functions que chamam por nome → sempre `grep` callers + re-deploy junto da migration.

### Em aberto
- **Config do agentId no fluxo N8N** (lado N8N, manual) — backend já pronto. Task `86e1k160z`.
- Time entries (50min) não registrados — cota de Advanced Time Tracking do ClickUp atingida (40/40).
- Commits já em `origin/main` (`5375190`); local está 2 commits atrás do remoto (campaigns) — fast-forward pendente.

### Tasks ClickUp tocadas
- ✅ `86e1k40vw` (comentário — RAG do catálogo finalizado)
- 🟡 `86e1k160z` (comentário — backend search-knowledge desbloqueado)

---

## Sessão 18:30 — ReabilitaCão: Fix sobreposição transcrição IA (GSD Phase 3)

### O que foi feito
- **GSD Phase 3 criada + executada** — proteção contra sobreposição de transcrição IA no `ConsultationForm.tsx`. A transcrição da IA não reseta mais a tela nem apaga anotações digitadas manualmente.
- **Dialog de conflito** (inline, 3 botões): quando `onTranscriptReady` ou `onRestoreTranscript` dispara com editor já preenchido → **Substituir** / **Adicionar ao final** / **Cancelar**. Editor vazio recebe transcrição direto, sem diálogo.
- Helper `applyTranscriptToContent(mode, current, html)` extraído; `pendingTranscriptRef` + `showTranscriptConflictDialog` adicionados; `transcriptJustSetRef` setado antes de `setContent` em todos os paths.
- Verifier GSD: 6/6 must-haves automáticos ✓, build OK. Status `human_needed` — 5 cenários runtime salvos em `03-HUMAN-UAT.md`.
- Commits: `3e679a2` (feat) + `a1ac586` (docs). Artefatos em `.planning/phases/03-fix-transcript-overwrite-consultation-form/`.
- **Investigação bug "gravação aparece em Nova Consulta":** diagnosticado como rascunho órfão do `handleSilentSave` (cria consulta + troca URL silenciosamente, título continua "Nova Consulta"). NÃO é bug de delete — CASCADE em `consultation_recordings` está correto.

### Decisões
- Sobreposição de transcrição resolvida com **diálogo de escolha** (não auto-substituir, não auto-append) — vet decide, anotação manual nunca é perdida silenciosamente.
- Rascunho órfão **não corrigido nesta sessão** (item [2] do validar-sessao recusado pelo usuário). Recomendação registrada: banner "Retomar rascunho" com opção de descartar órfão.

### Em aberto
- Validação manual dos 5 cenários da Phase 3 em runtime (`03-HUMAN-UAT.md`)
- Follow-up rascunho órfão de gravação (diagnóstico salvo no GSD, sem task no ClickUp)
- Push dos commits `3e679a2`+`a1ac586` (local, ainda não em origin)

### Tasks ClickUp tocadas
- 🆕 `86e1kxx4v` — ReabilitaCão — Fix: transcrição IA não sobrescreve anotações manuais (concluído; time entry 30min falhou — cota Advanced Time Tracking 40/40)
