# Sessão — 2026-05-01

## O que foi feito

### LP Eloscope — pré-deploy
- @architect (Aria) + @qa (Quinn) invocados em paralelo para análise de arquitetura
- 6 correções aplicadas: supabase/.gitignore, Twitter title, ORG_ID env var, dead code Form.tsx, react-imask removido, WhatsApp 5517920003580
- Build Next.js verificado ✅ — 5 rotas geradas
- Commit feito: `feat: LP Eloscope — implementação completa pronta para deploy` (35 arquivos)

### Favicon
- favicon.ico padrão Next.js removido (sobrescrevia icon.png)
- app/icon.png criado: logo Eloscope 512×512 via Python/Pillow, padding 4%, fundo transparente
- Logo fonte: `cerebro/areas/design/assets/logos/EloScope - Logo.png`

### GitHub + Vercel
- Repo git independente já existe em `eloscope-lp/`
- Aguarda `gh auth login` do usuário para criar remote e fazer push

## Decisões
- ORG_ID movido de hardcode para env var (`process.env.ORG_ID`)
- favicon = app/icon.png (Next.js App Router) sem favicon.ico
- LP repo independente do workspace pai — deploy isolado no Vercel

## Em aberto
- `gh auth login` → criar repo GitHub → push → conectar Vercel
- Adicionar env vars no Vercel: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `ORG_ID`
- Testar mobile responsivo
- Favicon aprovação final (tamanho ok?)

---

## Sessão tarde — LP Eloscope: testes de video background

### O que foi feito
- Testados ~12 vídeos de background no Hero (Mux HLS, Cloudflare HLS, CloudFront MP4)
- Abordagem de tint definida: CSS filter `grayscale(1) sepia(1) hue-rotate(155deg) saturate(1.5) brightness(0.45)` — sem overlay, sem mix-blend-mode
- Badge do Hero alinhado ao padrão glass do site: `#08090d` bg, `rgba(255,255,255,0.08)` border, glass top line, box-shadow interno
- Subtítulo: `text-muted` → `text-neural-white/60` (mais legível)
- Ambient glow reduzido para valores sutis
- Glow line base do Hero: brilho aumentado (`boxShadow: '0 0 30px 8px rgba(0,212,255,0.9)...'`)
- Video player: testadas 3 abordagens de loop (timeupdate seek, crossfade 2 vídeos, restart hls); ficou com restart hls no evento `ended`
- `video.src` no JSX corrigido: `VIDEO_URL` → `VIDEO_SRC` (erro de refactor)
- Video atual: `tLkHO1qZoaaQOUeVWo8hEBeGQfySP02EPS02BmnNFyXys` (Mux)

### Decisões
- **CSS filter** é a abordagem definitiva para tint de cor no vídeo (sem overlay, sem blend-mode — mais simples e sem vazamento)
- **HLS loop**: reiniciar instância hls.js no evento `ended` (mais simples que seek ou crossfade)
- **Badge glass** deve seguir exatamente o padrão dos outros cards do site

### Em aberto
- Loop HLS ainda tem breve flash no reinício — investigar alternativa (MP4 nativo seria ideal)
- Deploy Vercel (aguarda `gh auth login`)
- Mobile testing
