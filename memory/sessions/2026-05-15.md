# Sessão — 2026-05-15

## O que foi feito

- **ClickUp cleanup completo (semana 20):** marcadas como concluídas tarefas do Victor na ReabilitaCão (~16 tasks), carrosséis Instagram (4 subtarefas) e bio Instagram (2 subtarefas) — status "published" descoberto como correto para listas Redes Sociais e Guia de Modelos
- **Tarefas novas criadas no ClickUp (14 no total):** elobrain/gbrain (Gestão Interna), skills criadas (/elo-setup, /post-instagram, /buscar-memoria, /salve, carrossel-eloscope), PeleVet deck+discovery, Campos Joia (RAG + prompt + teste), Maqlam (playbook + SDR), brandbook, LP meta-ads-mcp, Sales OS playbook — todas com data 14/05 e atribuídas ao Victor
- **Automações Nath e Cris (ReabilitaCão) revertidas para "pendente"** após marcação incorreta como concluídas
- **Weekly Semana 20 preparada:** documento completo em `areas/reunioes/semanais/2026-05-15-semanal.md` com retrospectiva por pessoa, status dos clientes, bloqueios, "o que não funcionou", pontos a validar, métricas e action items
- **Documento sincronizado no elobrain:** slug `areas/reunioes/semanais/2026-05-15-semanal`

## Decisões

- Weekly de amanhã confirmada às **8h30** (não 16h como constava anteriormente)
- Status "published" é o correto para listas Redes Sociais e Guia de Modelos no ClickUp (descoberto por trial-and-error)

## Em aberto

- Victor preencher métricas antes da weekly (receita, ratio tarefas, dias lead→proposta)
- Victor validar resultado reunião PeleVet 11/05 — sem registro no brain
- Lucas confirmar recebimento Bravo 2ª parcela R$975 (vence hoje)
- Discutir na weekly: Jean, Pipeline CRM Lucas, Rotina Comercial Lucas
- Carrosséis Instagram: posts finais não revisados, não publicados
- Automações Nath e Cris (ReabilitaCão) ainda pendentes

---

## Sessão (madrugada) — setup MCP elobrain + auditoria gbrain

### O que foi feito

- **MCP elobrain registrado corretamente** via `claude mcp add --scope local` em `~/.claude.json`. Diagnosticado que `.mcp.local.json` não é formato lido pelo Claude Code (não bate no `claude mcp list`). Status agora: `✓ Connected`.
- **`.mcp.local.json` deletado** — chaves OpenAI/Supabase expostas no arquivo (e formato inútil); MCP agora vive em `~/.claude.json` (escopo local do projeto).
- **Auditoria gbrain × Supabase:** confirmado source `cerebro` no schema `brain` do Supabase Elosystem com **766 pages, 3372 chunks, last_commit `e4f5cf52` (= HEAD)**. Vault remoto está em dia. Mistério do `~/.gbrain/config.json` apontando pra `pglite` resolvido: o `.env` do repo tem `DATABASE_URL` com `search_path=brain,public` e **override o config.json** quando o `gbrain` roda no diretório do repo.
- **9 sync_failures stale removidas:** `rm ~/.gbrain/sync-failures.jsonl` — `gbrain doctor` voltou a 95/100 limpo.
- **Frontmatter validate dry-run:** 2321 errors mapeados (2290 MISSING_OPEN, 17 NESTED_QUOTES, 14 YAML_PARSE).
- **`gbrain frontmatter generate --fix`** rodado — reportou 2218 writes + 2218 `.bak`. **Problema:** abrangeu também `.aios-core/`, `.aiox-core/` e `.claude/` (framework L1/L2 protegido). Revertido via `git checkout` nesses dirs + 1789 `.bak` removidos. Sobrou só `.bak` no vault propriamente dito.
- **`*.bak` adicionado ao `.gitignore`** — 436 `.bak` ficam como backup local sem poluir o repo.
- **Investigação skill `/elo` v0.4:** commit `6adfb20` (Victor) só MENCIONA — implementação não está em `~/elobrain/skills/` nem `~/.claude/skills/`. Provavelmente vive em workspace `growth-os-skills` separado.

### Decisões

- **MCP project-scoped:** registrar via `claude mcp add --scope local`, não inventar `.mcp.local.json` (formato inexistente). Único `.mcp.json` válido é o project-shared (commit no git), e `~/.claude.json` carrega config local.
- **`gbrain frontmatter generate --fix` é ferramenta perigosa:** não roda sobre `.aios-core/` `.aiox-core/` `.claude/`. Escopo correto = só vault (memory/, areas/, empresa/, squads/, skills/, time/, agentes/, etc) ou flag explícita de path. Próxima execução: especificar pasta-alvo, não a raiz.
- **`*.bak` no `.gitignore`:** backups locais do `frontmatter generate` ficam fora do git.

### Em aberto

- **Reiniciar Claude Code novamente** para ativar tools `mcp__elobrain__*` no registry. Sem restart, MCP está conectado mas as tools não aparecem na sessão.
- **Testar query via MCP elobrain após restart:** ex. `mcp__elobrain__query "pendências críticas"`.
- **Rodar `gbrain frontmatter generate --fix` com escopo correto** (só vault de conteúdo) numa próxima sessão, se quiser fechar os 2290 MISSING_OPEN.
- **Localizar skill `/elo` v0.4** — está em workspace `growth-os-skills` ou outro repo do Victor; não está nos paths investigados.
- **Decidir 4 skillpacks recomendados** (article-enrichment, voice-note-ingest, perplexity-research, concept-synthesis) — não discutido nessa sessão.
