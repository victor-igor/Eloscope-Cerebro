# Sessão — 2026-05-27

## O que foi feito

- **Maqlam RAG — Waves 1–5 aplicadas:** embedding_queue + trigger, HNSW Inner Product, hybrid_search refatorado (websearch_to_tsquery + p_min_similarity 0.5), RPCs atômicas, chunking pipeline
- **process-embeddings deployada (v1):** função nunca havia sido deployada — era a causa raiz dos 404 no cron; agora roda a cada 2 min via pg_cron
- **search-knowledge simplificada (v22):** removida lógica de prompt/contextBlock, retorna apenas `{results, mode}`
- **5 novos registros RAG inseridos** (ids 146–150): fluxo geral, consumíveis, re-engajamento, alternativa econômica cortante, respostas rápidas — embeddings 1536d gerados
- **agent_ids atualizados** em 38 playbooks para incluir agente `767e8044-0856-4430-832f-8950b4c0da15`
- **Diagnóstico: N8N não envia agentId** → filter `visibility=private` exclui todos os playbooks — task criada `86e1k160z`
- **/validar-sessao:** 2 comentários no ClickUp (86e1jvzv5, 86e1d9gqx), 1 task criada (86e1k160z)

## Decisões

- `p_min_similarity` mantido em 0.5 (era 0.78 — muito restritivo) para o RAG de atendimento
- Arquitetura 4 tabelas (`ai_knowledge_base`, `ai_faqs`, `ai_team_schedules`, `ai_holidays`) mantida (vs single-table da referência) — decisão de manutenção da semântica por tipo

## Em aberto

- Configurar `agentId` no nó N8N da search-knowledge tool (`86e1k160z`, due 28/05)
- Testar busca pós-fix com query "lâmina quebrada" — deve retornar 5+ resultados
- `/push` para PR `feat/rag-wave-1` → `main`
- Preencher `[A PREENCHER]` no `rag-base.md` com dados reais (horários, endereço, formas de pagamento)

---

## Sessão [tarde] — Fix bug transbordo prompt SDR Maqlam

### O que foi feito
- **Comparação arquitetural BDR Campos Joias × SDR Maqlam:** mesmo template base (XML tags, regras numeradas, fluxo sequencial). Maqlam é versão mais evoluída — tem 3 seções extras (`<crm>`, `<deteccao-bot>`, `<anti-alucinacao>` dedicada) e 5 ferramentas vs 3 da Campos
- **Diagnóstico do bug "RESUMO PARA A EQUIPE vazando pro cliente":** causa raiz era prompt ambíguo na seção `<transbordo>` — não deixava explícito que o resumo é parâmetro da tool `transferencia_humano`, não texto visível ao cliente
- **Fix aplicado no system-prompt SDR Maqlam** (arquivo + elobrain, 5 chunks):
  1. `<transbordo>`: removida mensagem fixa hardcoded → 2 passos explícitos (Passo 1 mensagem natural ao lojista / Passo 2 resumo EXCLUSIVO da ferramenta)
  2. `<fluxo>`: encerramento "Fico à disposição..." → diretiva de comportamento
  3. `<fluxo>`: mensagem do loop sem progresso → diretiva apontando pra Regra 3
- **Princípio aplicado a pedido do Victor:** preferir diretivas de comportamento sobre mensagens fixas — o LLM gera a fala adequada ao contexto

### Decisões
- Mensagens fixas em prompts de agente → substituir por diretivas de direcionamento (mais natural, contextual)

### Tasks ClickUp tocadas
- ✅ `86e1b9gdd` — Agente Maq — ajustes (done, 30min)
- 🟡 `86e1d9gqx` — Maqlam — Voltar agente SDR em produção (comentário de avanço)

### Em aberto
- Testes end-to-end do SDR Maqlam antes de retornar à produção (`86e1d9gqx`)
