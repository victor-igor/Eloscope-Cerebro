---
tipo: sessao
data: 2026-05-13
tags: [elobrain, framework, supabase, embeddings, mcp]
---

# Sessão — 2026-05-13

## O que foi feito

- Análise completa do fork `victor-igor/elobrain` (fork público de `garrytan/gbrain` v0.33.0) — framework de segundo cérebro com hybrid search + pgvector + 34 skills + MCP server
- Decisão arquitetural: elobrain como **framework instalável** (não embedded); cerebro Eloscope é a aplicação que consome via MCP; mesmo padrão será replicado para clientes
- Aplicado o schema `brain` no Supabase Elosystem (`jaygqiooeolzachevvfa`) em 3 migrations:
  - `elobrain_01_core_tables` — sources, pages, content_chunks (vector 1536), links, code_edges
  - `elobrain_02_support_tables` — tags, raw_data, timeline_entries, page_versions, config, oauth, files
  - `elobrain_03_jobs_eval_rls` — minion_jobs, subagent, eval, RLS
  - Total: **32 tabelas** no schema `brain`, isoladas do `public`
- Validação end-to-end via Supabase MCP (insert page + chunk + FTS score 0.389 + vector search similarity 1.0 + cascade delete — todos OK)
- `.env` reescrito (109 → 28 linhas úteis) — DATABASE_URL com `search_path=brain,public`, OPENAI_API_KEY válida, credenciais Supabase preservadas, AUTENTIQUE_KEY e CLICKUP_API_KEY mantidos, boilerplate AIOX removido
- `.claude/settings.json` ganhou MCP server `elobrain` (`gbrain serve` com `GBRAIN_SOURCE=cerebro`)
- Skill `/salve` ganhou passo 6: `gbrain sync --source cerebro` com fallback defensivo (não bloqueia se gbrain não estiver instalado)
- Skill `/buscar-memoria` reescrita: hybrid search via MCP elobrain como backend primário, grep estruturado como fallback automático
- Memória de longo prazo gravada: `project_elobrain_setup.md` com arquitetura framework × aplicação
- Criado `memory/projects/elobrain.md` + entrada no `_index.md`

## Decisões

- **elobrain é framework instalável, não embedded** — fork público mantém branding gbrain genérico, cerebros (Eloscope e clientes) consomem via MCP
- **Schema `brain` dedicado no Supabase** — isolamento total do `public` (evita conflito com `public.tags` da plataforma Elosystem que é usada na view `api_integration_contacts_v2`)
- **Skills com fallback defensivo** — funcionam com ou sem o framework instalado; permite rolar pra produção sem dependência hard
- **Connection pooler com `options=-c search_path=brain,public`** — funciona via pgbouncer, não precisa de SET por sessão
- **Vai virar produto Eloscope** — "Cerebro Claude Code by Eloscope" será pacote: template repo + INSTALL_ELOBRAIN.md + migrations + .env.example + .claude/settings.json pré-config

## Em aberto

- 🔥 Instalar `gbrain` localmente na máquina Lucas (clone `victor-igor/elobrain` + `bun install` + `bun link`)
- Criar `INSTALL_ELOBRAIN.md` runbook reaproveitável (pré-requisito do produto futuro)
- Primeira indexação: `gbrain sync --source cerebro` — gera embeddings do vault inteiro (~$0.02 OpenAI)
- Reiniciar Claude Code para carregar MCP `elobrain`
- (Backlog) Empacotar produto "Cerebro Claude Code by Eloscope" para venda a clientes

## Arquivos tocados

- `.env` — limpeza + DATABASE_URL/DIRECT_URL com search_path
- `.claude/settings.json` — MCP server elobrain
- `skills/salve/SKILL.md` — passo 6 gbrain sync
- `skills/buscar-memoria/SKILL.md` — reescrita para hybrid search + fallback grep
- `memory/context/decisoes/2026-05.md` — 2 decisões novas no topo
- `memory/context/pendencias.md` — seção "elobrain — framework instalável" criada
- `memory/projects/elobrain.md` — novo projeto
- `memory/projects/_index.md` — entrada elobrain + "Mudanças Recentes"
- `~/.claude/projects/.../memory/MEMORY.md` + `project_elobrain_setup.md` — memória longo prazo
