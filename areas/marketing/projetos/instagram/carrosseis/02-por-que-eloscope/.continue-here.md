---
phase: carrossel-02-design
task: 4
total_tasks: 6
status: in_progress
last_updated: 2026-04-16T22:00:00.000Z
---

<current_state>
Sistema visual aprovado. Watermark do símbolo Eloscope implementado no slide 02.
Header padronizado no HTML mas LOGO NÃO APARECE em todos os slides — precisa de script unificado.
Próxima sessão: padronizar geração de todos os slides com header correto.
</current_state>

<completed_work>
- Slide 01 (capa): 01-capa.html + 01-capa.png ✓
- Slide 02 (mercado): 02-mercado.html + 02-mercado.png ✓ (watermark símbolo implementado)
- Slide 03 (falha): 03-falha.html + 03-falha.png ✓ (precisa header correto)
- Slide 04 (mecanismo): 04-mecanismo.html + png ✓ (precisa header correto)
- Slide 05 (padrão): 05-padrao.html + png ✓ (precisa header correto)
- Slide 06 (CTA): 06-cta.html + png ✓ (precisa header correto)
- Estrutura de pastas: html/ png/ assets/ ✓
- Watermark símbolo Eloscope: monocromatico.png invertido via jimp → /tmp/symbol-inverted.png
</completed_work>

<watermark_sistema>
## Watermark do símbolo Eloscope

Arquivo original: /Users/victorigor/assistente/eloscope-brandbook/07-arquivos/logos/monocromatico.png
Arquivo processado (invertido): gerado em runtime via jimp

### No script de geração (antes do puppeteer):
```javascript
const { Jimp } = require('jimp');  // npm install jimp em /tmp
await Jimp.read(MONO_LOGO_PATH).then(img => img.invert().write('/tmp/symbol-inverted.png'));
const symbolData = fs.readFileSync('/tmp/symbol-inverted.png').toString('base64');
```

### CSS no HTML:
```css
.symbol-watermark {
  position: absolute;
  width: 960px;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  z-index: 8;
  opacity: 0.18;
  pointer-events: none;
}
.symbol-watermark img { width: 100%; height: auto; display: block; }
```

### HTML:
```html
<div class="symbol-watermark"><img id="symbol-wm" alt=""></div>
```

### Injeção Puppeteer:
```javascript
const symbol = document.getElementById('symbol-wm');
if (symbol) symbol.src = 'data:image/png;base64,' + symbolB64;
```
</watermark_sistema>

<header_problema>
## Problema identificado

O `<img id="brand-icon">` existe em todos os HTMLs mas fica vazio.
O src só é injetado pelo script Puppeteer no momento da geração.
Slides 04-06 foram gerados com scripts antigos sem injeção correta.

## Arquivos HTML existentes:
- html/01-capa.html
- html/02-mercado.html
- html/03-falha.html
- html/04-mecanismo-white.html  (versão alternativa)
- html/04-mecanismo.html
- html/05-padrao.html
- html/06-cta.html
</header_problema>

<next_action>
## PRÓXIMA SESSÃO: Padronizar header + regenerar todos os slides

### Passo 1: Criar /tmp/gen-all-slides.js
Script unificado que:
1. Lê fontes: /tmp/fonts.css (ou rebuild via /tmp/rebuild-fonts.js)
2. Inverte símbolo via jimp: monocromatico.png → /tmp/symbol-inverted.png
3. Para cada slide (01-06):
   - Lê o HTML correspondente
   - Substitui @import do Google Fonts por fontCSS base64
   - Injeta via page.evaluate: brand-icon, symbol-wm, lens-img (se existir)
   - Screenshot → salva PNG na pasta png/

### Assets a injetar em todos os slides:
- Logo header: /Users/victorigor/assistente/eloscope-brandbook/07-arquivos/logos/EloScope - Logo.png
  → id="brand-icon"
- Símbolo watermark: /tmp/symbol-inverted.png (pré-processado)
  → id="symbol-wm"
- Imagem 3D (só slides 02-05): assets/Por que a Eloscope.png
  → id="lens-img"

### Header padrão (já no HTML de todos os slides):
```html
<div class="header">
  <div class="brand-mark">
    <img id="brand-icon" class="brand-icon-img" alt="">
    <span class="brand-name">eloscope</span>
  </div>
  <div class="handle">@eloscope.ai</div>
</div>
```

### CSS header padrão (já nos HTMLs):
```css
.header { position: absolute; top: 52px; left: 56px; right: 56px;
  display: flex; justify-content: space-between; align-items: center; z-index: 20; }
.brand-mark { display: flex; align-items: center; gap: 10px; }
.brand-icon-img { height: 24px; width: auto; mix-blend-mode: screen; }
.brand-name { font-family: 'Syne'; font-weight: 800; font-size: 17px;
  letter-spacing: 0.15em; color: #00D4FF; }
.handle { font-family: 'JetBrains Mono'; font-size: 13px;
  color: rgba(107,114,128,0.8); letter-spacing: 0.03em; }
```

### Passo 2: Verificar HTMLs 04-06
- Verificar se 04-mecanismo.html, 05-padrao.html, 06-cta.html têm o sistema visual correto
  (imagem 3D como bg + gradiente + texto bottom-left + watermark)
- Se ainda forem layout antigo (text-only centrado) → redesenhar antes de gerar

### Passo 3: Regenerar todos os 6 slides com script unificado
</next_action>

<decisions_made>
- Formato: 1080×1350px portrait
- Fontes: embeddar como base64 (NUNCA Google Fonts @import no Puppeteer)
- Syne 800 + Syne 700 + Inter 400/500/600 + JetBrains Mono
- Puppeteer: page.goto('file:///tmp/...', { waitUntil: 'load' })
- Logo header: EloScope - Logo.png (mix-blend-mode: screen, height: 24px)
- Watermark símbolo: monocromatico.png invertido via jimp, opacity: 0.18, z-index: 8
- Imagem 3D slides: assets/Por que a Eloscope.png (mesma para todos por agora)
- Sistema visual: imagem 3D topo/direita como bg + gradiente escuro + texto bottom-left
- Pills: Inter 600, full-width, rgba(255,255,255,0.05) bg, borda rgba(255,255,255,0.14)
- NÃO usar CSS filter no Puppeteer (causa screenshot timeout na imagem 1080x1350)
- Jimp deve ser instalado em /tmp: cd /tmp && npm install jimp
</decisions_made>

<pipeline_de_geracao>
BASE='/Users/victorigor/assistente/eloscope-brandbook/marketing-conteudo/instagram/carrosséis/02-por-que-eloscope'
LOGOS='/Users/victorigor/assistente/eloscope-brandbook/07-arquivos/logos'

1. Verificar fontes:
ls /tmp/fonts.css 2>/dev/null || node /tmp/rebuild-fonts.js

2. Instalar jimp se necessário:
ls /tmp/node_modules/jimp 2>/dev/null || (cd /tmp && npm install jimp)

3. Gerar slide específico:
cd /tmp && node gen-all-slides.js 02

4. Gerar todos:
cd /tmp && node gen-all-slides.js all
</pipeline_de_geracao>
